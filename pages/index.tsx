import { UserContext } from "@/context/user";
import Head from "next/head";
import Image from "next/image";
import Game from "@/components/GameLocal";
import { useEffect, useState, useContext } from "react";
import { SocketContext } from "../context/socket";
import styles from "../styles/Home.module.scss";
import Board from "@/components/Board";
import { Color } from "@/util/chess";
import NonSSRWrapper from "@/components/NonSSRWrapper";
export default function Home() {
  // const [connected, setConnected] = useState(false);
  // const socket = useContext(SocketContext);
  // const user = useContext(UserContext);
  // useEffect(() => {
  //   socket.on("connect", () => {
  //     setConnected(socket.connected);
  //   });
  //   setConnected(socket.connected);
  //   return () => {
  //     socket.off("connect");
  //   };
  // }, []);

  return (
    <div
      style={{
        backgroundImage: `url("/assets/background.png")`,
        backgroundRepeat: "no-repeat",
        backgroundSize: "cover",
        overflowX: "hidden",
        backgroundPosition: "bottom 0 left 0",
      }}
      className="flex w-screen h-screen justify-center items-center "
    >
      <div className="h-full w-full justify-center items-center flex bg-black/[0.1]">
        <Head>
          <title>Next-Chess</title>
          <meta name="description" content="Generated by create next app" />
          <link rel="icon" href="/favicon.ico" />
        </Head>

        <main className="flex container justify-center items-center w-full h-full">
          <div className="h-[80%] max-w-[120vh] w-full  bg-[#1f1f1f]/[0.5] backdrop-blur-lg  shadow-[0px_30px_60px_-10px_rgba(150,150,150,0.1)] rounded-lg p-10">
            <h1 className="text-6xl text-white ">Next Chess</h1>
          </div>
        </main>
      </div>
    </div>
  );
}
